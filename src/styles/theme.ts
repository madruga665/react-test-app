export interface ITheme {
  colors: {
    primaryBlue: string;
    wetAsphalt: string;
    concret: string;
    red: string;
    pageBackground: string;
    white: string;
  };
}

export const theme = {
  colors: {
    primaryBlue: "#3498db",
    wetAsphalt: "#34495e",
    concret: "#95a5a6",
    red: "#e74c3c",
    pageBackground: "#ecf0f1",
    white: "#fff",
  },
};
